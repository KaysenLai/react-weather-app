import React from 'react';
import './WeatherCard.scss';

const WeatherCard = () => {
  return (
    <main className="weather-card">
      <div className="weather-card__content"></div>
      <div className="weather-card__sidebar"></div>
    </main>
  );
};

export default WeatherCard;
